<%= form_for(@user, url: registration_path(@user), html: { class: 'login'} ) do |f| %>
  <fieldset>
    <legend class="legend">新規登録</legend>
    <div class="input">
      <%= f.email_field :email, autocomplete: "email", placeholder: 'メールアドレスを入力してください' %>
      <span><i class="far fa-envelope"></i></span>
    </div>
    <div class="input">
      <%= f.password_field :password, autocomplete: "off", placeholder: 'パスワードを入力してください' %>
      <span><i class="fa fa-lock"></i></span>
    </div>
    <div class="input">
    <%= f.password_field :password_confirmation, autocomplete: "off", placeholder: '確認のためパスワードをもう一度入力してください' %>
      <span><i class="far fa-check-circle"></i></span>
    </div>

    <div class="row mb-5">
      <div class="col-md-8 mx-auto">
        <%= link_to 'ログインページはこちら', new_user_session_path, class: 'btn btn-dark btn-lg' %>
      </div>
    </div>
    <button type="submit" class="submit"><i class="fas fa-sign-in-alt"></i></button>
  </fieldset>
  <div class="feedback">
    login successful <br />
    redirecting...
  </div>
<% end %>
